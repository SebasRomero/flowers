export enum Role {
  AGENT = 'agent',
  ADMIN = 'admin',
  OWNER = 'owner',
}

export enum RoleAgent {
  AGENT = 'agent',
}

export enum RoleOwner {
  OWNER = 'owner',
}

export enum RoleAdminAGent {
  ADMIN = 'admin',
  AGENT = 'agent',
}

export const RoleAgentArray = ['agent'];
export const RoleAdminArray = ['admin'];
export const RoleOwnerArray = ['owner'];
